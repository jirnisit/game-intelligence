export type Text = { en?: string; th?: string };
export type Buff = { id: string; stat_code: string; target: string; value: number | string | null; unit: string | null; per_stack: boolean; name?: Text; };
export type Character = { id: string; name: Text; class_name: Text; element_name: Text; rarity: string; element_code: string; class_code: string; race: Text; notes: Text; has_hold: boolean; buffs: Buff[] };
export type Effect = Buff & { required_statuses: {code: string; providers: {character_id: string; character_name: Text; skill_name: Text; description: Text; target: string; stacks: number|null}[]}[]; effect_type: string; status_id: string | null; skill_id: string | null; status_name: Text; skill_name: Text; directly_granted: boolean; condition: Record<string, unknown>; scaling_stat: string | null; scaling_character_id: string | null; element_code: string | null; applications: {name: Text; description: Text; stacks: number | null; target: string}[] };
export type Detail = Character & { awakening: number; skills: { id: string; category: string; name: Text; description: Text; has_hold: boolean; cooldown_seconds: string|null }[]; effects: Effect[]; awakenings: {level: number; name: Text; description: Text; unlocked: boolean}[]; statuses: {id: string; name: Text; completeness: string; rule: null | {max_stacks: number | null; duration_kind: string; duration_seconds: number | string | null; refresh_on_stack: boolean|null; mechanics: Record<string, unknown>}}[] };
export type Meta = { games: {id:string;name:Text}[]; elements: {game_id:string;code:string;name:Text}[]; classes: {game_id:string;code:string;name:Text}[]; buffStats:string[] };
